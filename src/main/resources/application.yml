spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
  h2:
    console:
      enabled: true
  Security:
    oauth2:
      client:
        registration:
          google:
            client-id: random_id
            client-secret: random_secret
            scope:
              - email
              - profile


jwt:
  issuer: sj7934@gmail.com
  secret_key: random_key
